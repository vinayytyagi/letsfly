/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.pexels.com', 'randomuser.me', 'i.imgur.com', 'wdtletsgo.wpengine.com'],
  },
};

module.exports = nextConfig;
